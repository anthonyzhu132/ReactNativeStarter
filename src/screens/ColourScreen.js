import React, { useState } from "react";
import { View, StyleSheet, Button } from "react-native";

const ColourScreen = () => {
  return (
    <View>
      <Button title="Add Colour"/>
    </View>
  );
}

const styles = StyleSheet.create({});

export default ColourScreen;